from fastapi import APIRouter

api_router = APIRouter()

# As rotas de versão (v1) serão incluídas aqui conforme forem criadas
# Exemplo: api_router.include_router(auth.router, prefix="/auth", tags=["auth"])
